# Workspace Agent Rules

- Always use the web search tool to find the latest data and ideas instead of relying on your internal dataset.
- The 'second-brain' directory is located at `D:\second-brain`. Whenever the user mentions 'second brain' or 'second-brain', always refer to this directory.
