"""Phase 5 placeholder — OAuth 2.1 for remote MCP (enterprise)."""

from __future__ import annotations

# Future: OAuth 2.1 discovery at /.well-known/oauth-authorization-server
# Resource indicators, PKCE, scoped tokens per client namespace.
# Required only when exposing Streamable HTTP beyond loopback.

PHASE = 5
STATUS = "planned"
